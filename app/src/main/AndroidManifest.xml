<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cintaxedge.tutoring">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tutoring">
        <activity android:name=".Activities.PracticeQuestionNewActivity"></activity>
        <activity android:name=".Activities.FlashCardsActivity" />
        <activity android:name=".Activities.WebviewActivity" />
        <activity android:name=".Activities.PracticeQuestionActivity" />
        <activity android:name=".Activities.LessonQuestionsActivity" />
        <activity android:name=".Activities.LessonDashboardActivity" />
        <activity android:name=".Activities.LessonsActivity" />
        <activity android:name=".Activities.TermandConditionsActivity" />
        <activity android:name=".Activities.ResultsActivity" />
        <activity android:name=".Activities.ProfileActivity" />
        <activity android:name=".Activities.SubscribeActivity" />
        <activity android:name=".Activities.ChangePasswordActivity" />
        <activity
            android:name=".Activities.BottomNavigaionAcivity"
            android:label="@string/title_activity_bottom_navigaion_acivity" />
        <activity android:name=".Activities.ForgetpasswordActivity" />
        <activity android:name=".Activities.RegisterActivity" />
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.MainActivity" />
    </application>

</manifest>